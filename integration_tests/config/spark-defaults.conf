# Static Spark configuration for integration tests.
# Delta catalog, Hive metastore, and reasonable resource defaults.

# Catalog configuration
spark.sql.catalogImplementation=hive
spark.sql.catalog.spark_catalog=org.apache.spark.sql.delta.catalog.DeltaCatalog
spark.sql.extensions=io.delta.sql.DeltaSparkSessionExtension
spark.sql.sources.default=delta

# Delta schema options
spark.databricks.delta.schema.autoMerge.enabled=true

# Resource allocation (fixed for CI — no template rendering)
spark.driver.memory=2g
spark.driver.cores=2
spark.executor.memory=2g
spark.executor.cores=2
spark.sql.shuffle.partitions=4
spark.default.parallelism=4

# Performance — AQE and single-machine optimizations
spark.sql.adaptive.enabled=true
spark.sql.adaptive.coalescePartitions.enabled=true
spark.locality.wait=0s

# Warehouse location
spark.sql.warehouse.dir=/opt/spark/warehouse
