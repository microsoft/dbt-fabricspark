version: 2

models:
  - name: dim_product
    columns:
      - name: productid
        description: The natural key of the product
        tests:
          - unique
      - name: product_name
        description: The product name
        tests:
          - not_null

